## TestCafe / Angular example of UI automation

### Quick Start

* Install dependencies by running `npm install`.
* Run tests in chrome by running `npm run test`
* Run tets in chrome and firefox by running `npm run testAll`